name: pre-commit checks
on: [push, pull_request]
jobs:
#  pre-commit:
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v3
#      - uses: actions/setup-python@v5
#        with:
#          python-version: "3.x"
#      - run: pip install pre-commit
#      - run: pre-commit run --all-files

#  analyse-dockerfile:
#    name: Checkov
#    runs-on: ubuntu-latest
#    container: bridgecrew/checkov:latest
#    steps:
#      - name: checkout repository
#        uses: actions/checkout@v3
#      - name: Install python
#        uses: actions/setup-python@v5
#        with:
#          python-version: "3.12"
#      - name: checkout repository
#        uses: actions/checkout@v3
#
#      - name: Run Checkov
#        run: |
#          checkov -d . -o junitxml | tee checkov.test.xml
#
#      - name: Upload Checkov report
#        uses: actions/upload-artifact@v4
#        with:
#          name: checkov-report
#          path: checkov.test.xml
#  build-image:
#    runs-on: ubuntu-latest
#    steps:
#      - name: auth
#        uses: docker/login-action@v3
#        with:
#          username: ${{ vars.DOCKER_USERNAME }}
#          password: ${{ secrets.DOCKER_KEY }}
#
#      - name: checkout repository
#        uses: actions/checkout@v3
#      - name: Set up QEMU
#        uses: docker/setup-qemu-action@v3
#      - name: Set up Docker Buildx
#        uses: docker/setup-buildx-action@v3
#      - name: Build and push
#        uses: docker/build-push-action@v6
#        with:
#          context: ./backend
#          file: ./backend/Dockerfile
#          push: true
#          tags: nolric/tests_eadl:backend_latest
#
#      - name: Utilisation Trivy
#        uses: aquasecurity/trivy-action@0.33.1
#        with:
#          scan-type: image
#          image-ref: 'docker.io/nolric/tests_eadl:backend_latest'
#          format: 'table'
#          exit-code: '1'
#          ignore-unfixed: true
#          vuln-type: 'os,library'
#          severity: 'CRITICAL,HIGH'

  hadolint_control:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repository
        uses: actions/checkout@v3
      - uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: backend/Dockerfile
          format: 'json'
          output-file: '.resultHadolint.json'
        continue-on-error: true
      - run: ls -la
      - uses: actions/upload-artifact@v5
        with:
          path: '.resultHadolint.json'


# Checkov, hadolint, trivy + Construction d'image
